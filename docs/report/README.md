# Internship report

`internship_report.tex` and `internship_report.pdf` document the mathematical
background, development history, algorithms, and validation examples for the
project. Paths shown inside the report describe the repository layout at the
time of the internship snapshot; the current package entry point is
`src/PlotCurveTopologyForBivariatePolynomial.jl`.

Temporary LaTeX build products belong in `build/` and are ignored by Git.
