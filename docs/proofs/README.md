# Mathematical proofs

This directory contains the LaTeX sources and rendered PDFs developed alongside
the implementation:

- the resultant/GCD criterion;
- algebraic numbers forming a field;
- constancy of the number of real roots between projection points;
- local horizontal separation of branches near a projection point.

The `.tex` files are the editable sources. The PDFs are retained as convenient
rendered artifacts.
