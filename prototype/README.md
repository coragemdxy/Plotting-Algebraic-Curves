# Prototype implementations

`rational_method/` preserves the original Method 1 development sequence. Its
files intentionally remain standalone snapshots, including duplicated helpers
and diagnostic output. They are excluded from the active package module so
their historical variants cannot overwrite the certified Method 2 interfaces.

The active implementation is under `src/`.
